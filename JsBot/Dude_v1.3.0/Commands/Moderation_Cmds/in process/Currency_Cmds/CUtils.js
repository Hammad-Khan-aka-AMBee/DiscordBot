//const person = {};
/*const Discord = require('discord.js');

module.exports = {
    ping : (message)=>{
        message.channel.send('Pong.')},
    beep : (message)=>{
        message.channel.send('Boop.')},
    
    userInfo : (message)=> {
        var c = require.toString('./cmd.json')
        message.channel.send(c)},
    info: (message)=>{
        message.channel.send('ping boop serverinfo avatar av')},
    userinfo : (message)=> {
		message.channel.send(`Your username: ${message.author.username}
        \nYour ID: ${message.author.id}
        \nUser Joined: ${message.author.createdAt}
        \ntag: ${message.author.tag}
        \nUser Joined The server: ${message.guild.joinedAt}
        \nDM Status : ${message.author.dmChannel}
        \nLast message sent:${message.author.lastMessage}
        \nUser AvatarURL: ${message.author.displayAvatarURL({ format: "png", dynamic: true })}
        \nUser Roles: ${message.author.roles}`);
    },
    serverinfo : (message) =>{
		message.channel.send(`This server's name is: ${message.guild.name}
        \nTotal members: ${message.guild.memberCount}
        \nGuild Region: ${message.guild.region}`);
    },
    /*avatar : (message)=> {
        return message.channel.send(`Your avatar:  ${message.author.displayAvatarURL({ format: "png", dynamic: true })}`);
		
    },
    av : (message) => {
        message.channel.send(
        new Discord.MessageEmbed()
        .setImage(message.author.displayAvatarURL({ format: "png", dynamic: true }))
        .setColor('#ff1167')
        .setDescription('\n')
        )
    },*/
    /*
    _ann : (message) => {
        message.channel.send(
        new Discord.MessageEmbed()
        .setImage('https://p.kindpng.com/picc/s/189-1893275_art-transparent-background-announcement-png-png-download.png')
        .setColor('#ff6767')
        .setDescription('\n')
        )
    },
    hello :(message)=>{ message.channel.send(
    new Discord.MessageEmbed()
    .setThumbnail('https://encrypted-tbn0.gstatic.com/images?q=tbn%3AANd9GcSv4avkIMX2ZdY4co02bpnnoql_5dBb_d93oA&usqp=CAU')
    .setColor('#ff004d' )
    .setDescription('Enjoy your Stay!\n We Welcome You in our Wonderful Server\n Be wholesome and Be happy :yum:'))
},
_here : (message)=>{ message.channel.send(
    new Discord.MessageEmbed()
    .setThumbnail('https://storage.needpix.com/rsynced_images/attention-156540_1280.png')
    .setTitle(`${message.author.send }`)
    .setFooter(`${message.content}`))
*/